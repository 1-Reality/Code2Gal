intro:在 WebGAL 的世界里 Code2Gal 与 Python 正在检查演出系统|performance.py 的确定性编译流程;
changeBg:bg.webp;
bgm:s_Title.mp3;
;[repo2gal performance] cue000001 b000001
changeFigure:character-guide-normal.png -transform={"position":{"x":-8.697,"y":326.35},"scale":{"x":1.538,"y":1.538},"alpha":0} -duration=0 -id=fig-guide -next;
setTransform:{"position":{"x":-8.697,"y":326.35},"scale":{"x":1.538,"y":1.538},"alpha":1} -duration=500 -target=fig-guide -parallel;
setTempAnimation:[{"duration":0,"scale":{"x":1.538,"y":1.538}},{"duration":600,"scale":{"x":1.63,"y":1.63}},{"duration":600,"scale":{"x":1.538,"y":1.538}}] -target=fig-guide -parallel;
say:演出系统的核心模块 performance.py 正在运行;
Code2Gal: Python 你看 extract_beats 把清洗后的脚本变成了 Beat Manifest;
Python: 每个 Beat 都带着 source_line 和 statement_index 还有 state_before 快照;
Code2Gal: state_before 记录了这条台词出现前舞台上的背景立绘和音乐;
Python: 就像开演前的舞台定点 每一个 beat 都精确知道自己身处的环境;
Code2Gal: 如果剧本有 label 和 choose 分支 状态快照就需要合并;
Python: _merge_incoming_states 会检查所有前驱路径的背景和音乐是否一致;
Code2Gal: 如果不一致它就把那个状态置为 None 表示这里无法确定;
Python: 对于角色立绘 如果所有前驱都一致就保留 否则加入 ambiguousFigures;
Code2Gal: 这样后续的演出验证就不会在不安全的角色上安排动作;
Python: extract_beats 还维护了 fallthrough_reachable 标记 label 后的直落是否可达;
Code2Gal: choose 命令会切断直落 而 jumpLabel 会根据有没有 when 参数决定;
Python: 这个状态机让 Beat Manifest 即使面对复杂分支也能保持稳定;
;[repo2gal performance] cue000002 b000014
changeBg:WebGalEnter.webp -enter=shockwaveIn -enterDuration=1200;
say:现在聚焦到演出计划的校验与编译;
Code2Gal: load_plan 会解析 LLM 返回的 JSON 并把 storyHash sceneId profile 绑定到本地上下文;
Python: 这样即使模型复制了长哈希 也不会让一个本来可用的计划失效;
Code2Gal: validate_plan 接着做语义检查 比如角色立绘是否存在 转场只能绑定一次;
Python: 它还检查 budget 限制 比如 maxCuesPerBeatRatio 和 maxActionsPerCue;
Code2Gal: 通过后 compile_plan 生成 PerformanceInsertion 列表;
Python: merge_insertions 把这些插入到原脚本 beat 的前后 或者 patch changeBg;
Code2Gal: _patch_change_bg 会替换掉已有的 enter exit 参数 再附加转场参数;
Python: 这样动态演出不会破坏原剧本 而是作为额外的舞台指令注入;
Code2Gal: 还记得 capability_hash 吗 它把能力表哈希成固定值 保证确定性;
Python: 任何环境变化都会被这个哈希捕获 让计划无法假装它适用于别的引擎;
say:两条叙事路径在你面前展开;
choose:先看状态合并的细节:labelMerge|先看编译插入的细节:labelCompile;
label:labelMerge;
;[repo2gal performance] cue000003 b000026
changeBg:background-archive.png -enter=shockwaveIn -enterDuration=1200;
Code2Gal: 我们回到分支汇合的状态合并;
Python: 当多个 label 指向同一个汇合点 每个来源的状态都会作为 incoming 传入;
Code2Gal: 如果某个角色的立绘在所有来源中相同 那么汇合后就保持这个立绘;
Python: 如果有一个来源里这个角色已经退场 另一个来源里它还站着 就会进入 ambiguousFigures;
Code2Gal: 这种角色在后续演出中会被禁止安排任何动作 因为系统不知道它的真实状态;
Python: 这就是为什么演出计划在语义校验时会检查 ambiguousFigures 并拒绝使用该角色;
Code2Gal: 不确定性被显式标记 而不是被忽略 这正是这个模块可靠的地方;
jumpLabel:labelJoin;
label:labelCompile;
;[repo2gal performance] cue000004 b000033
changeBg:background-community.png -enter=shockwaveIn -enterDuration=1200;
Code2Gal: 我们来看看编译插入的细节;
Python: compile_plan 会按 cue 的 beat 顺序遍历 为每个分支维护独立的运行时状态;
Code2Gal: 对于 screen.transition 动作 它不会立刻生成行 而是生成一个 background 锚点的插入;
Python: merge_insertions 找到对应的 changeBg 语句 然后调用 _patch_change_bg 附加转场参数;
Code2Gal: 这样转场效果紧贴在背景切换上 而不是作为一个孤立命令;
Python: 对于 figure.move 它会计算 slot 偏移和 framing 偏移 最终生成 setTransform;
Code2Gal: 对于 figure.shake 它生成一组关键帧 用 setTempAnimation 实现;
Python: 这些命令全部由 Python 确定性生成 不依赖任何随机数或时间;
jumpLabel:labelJoin;
label:labelJoin;
;[repo2gal performance] cue000005 b000041
changeBg:bg.webp -enter=shockwaveIn -enterDuration=1200;
Code2Gal: 无论你刚才看了哪一条路径 终点都是同一个确定性编译链;
Python: 从 Beat Manifest 到 Performance Plan 再到 WebGAL 命令 每一步都有校验;
Code2Gal: PerformanceReport 会记录 schema 是否有效 语义是否有效 以及是否降级;
Python: 如果 --strict 开启 任何降级都会导致退出码 5 拒绝打包;
Code2Gal: 我们内置了基准计划 build_baseline_plan 即使 LLM 失败也不会产出完全静态的场景;
Python: 它至少会为第一个有立绘的 beat 安排一个动画 或者为第一个有背景的 beat 安排转场;
Code2Gal: 这个系统把不可靠的自然语言生成 压缩进一个确定性的编译器框架;
Python: 就像给戏剧舞台安装了钢架 每一根梁的位置都有图纸可查;
say:演出系统继续运转 为下一个仓库准备编年史;
bgm:bgm-archive.ogg;
Code2Gal: 我们当前处理的 performance.py 只是 Code2Gal 的一个模块 但它的设计已经足够稳健;
Python: 未来的多场景剧情 多章节切分 还需要这个状态机和插入器继续扩展;
Code2Gal: 但今天 我们至少证明了源码可以成为一场可控的演出;
Python: 而确定性 就是这场演出不会走火的唯一保证;
end;
