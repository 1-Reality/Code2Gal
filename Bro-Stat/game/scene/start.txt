intro:Bro Stat 增强组件考古计划|从当前源码开始阅读;
bgm:s_Title.mp3;
changeBg:WebGalEnter.webp;
changeFigure:character-guide-normal.png -transform={"position":{"x":-8.697,"y":326.35},"scale":{"x":1.538,"y":1.538}} -id=fig-guide;
;[repo2gal performance] cue000001 b000001
setTempAnimation:[{"duration":0,"scale":{"x":1.538,"y":1.538}},{"duration":600,"scale":{"x":1.63,"y":1.63}},{"duration":600,"scale":{"x":1.538,"y":1.538}}] -target=fig-guide -parallel;
say:这不是普通的社区流水账;
say:当前仓库文本是第一证据;
say:源码快照是最高优先级;
BroStat:我醒来的时候，仓库里有五份用户脚本;
BroStat:TP L1 小米 RD 华为脚本 H3C TX1801 华硕脚本;
BroStat:还有 AGPL 3.0 协议 两份 README 三段历史快照;
BroStat:主语言是 JavaScript;
JavaScript:你终于开始读我了;
JavaScript:别急着看提交记录;
JavaScript:先看当前源码真正干了什么;
say:切换到社区档案;
;[repo2gal performance] cue000002 b000012
changeBg:background-community.png -enter=shockwaveIn -enterDuration=1200;
say:这里保留了 PR 和讨论的回声;
1Reality:我做过华为支持 ASUS 信号修复 多品牌同步;
1Reality:还有例行更新 把小米 5.9.4 改名成 5.9.6;
ucxn:TP 离线修复是我 文档说明是我 首次自动导出也是我;
HankowBroTech:更新逻辑从这里开始;
boli0301:Patch 1 到此一游;
say:这些是真实发生过的事 但真正的堡垒在源码里;
JavaScript:回到引擎核心;
;[repo2gal performance] cue000003 b000020
changeBg:bg.webp -enter=shockwaveIn -enterDuration=1200;
JavaScript:每份脚本都有自己的状态对象 S;
JavaScript:S 保存瞬时速率 累积流量 每台设备的积分和环形缓冲;
BroStat:采样函数是 rSD;
JavaScript:对 rSD 是采样链的主循环;
JavaScript:它用 window.__gIsF 防止重入;
BroStat:数据进来以后呢;
JavaScript:梯形积分;
JavaScript:旧速率加新速率 乘时间差 再乘 0.0005;
JavaScript:如果旧速率是 0 新速率是正数 就用半个刷新周期做 0 估算;
BroStat:这就是 0 估算的来历;
JavaScript:对 它让休眠到唤醒的瞬间流量不被漏掉;
JavaScript:每台设备还有火花线;
BroStat:火花线是什么;
JavaScript:hU 和 hD 是 Float64Array 环形缓冲;
JavaScript:SPRK 数组把速率映射成字符画波形;
BroStat:没有第三方图表库;
JavaScript:没有 纯字符画 峰值悬停 底噪过滤;
JavaScript:总速率图也是自己画的 Canvas;
BroStat:总速率图;
JavaScript:总上行图和总下行图是 Float64Array 8192;
JavaScript:可拖拽 可缩放的悬浮图表;
BroStat:还有别的路由器吗;
JavaScript:华为有 RSSI 漏桶防抖;
JavaScript:在负 23 负 34 负 40 负 55 这些边界憋住不闪;
JavaScript:H3C 有 GBK 解码 还有设备指纹快慢车道;
JavaScript:ASUS 每包 getTraffic 都是一次真实采样;
BroStat:五份脚本 五套接入逻辑 一条积分命脉;
BroStat:两条路由器脚本的数据获取方式完全不同;
BroStat:你想先看哪一条;
choose:TP 混合包探测:branch_tp|小米微分引擎:branch_mi;
label:branch_tp;
say:进入 TP L1 的 readTpStats;
JavaScript:TP 先尝试混合包 把 wan_status 和 online_host 放进同一个请求体;
JavaScript:error_code 是 0 而且两个表都在 就把 readTpStats 替换成混合包函数;
JavaScript:否则退回双包 一次拉 WAN 一次拉设备;
BroStat:还有 stok 捕获;
JavaScript:对 XMLHttpRequest.prototype.open 被钩住;
JavaScript:从 URL 里匹配 stok 参数 存到 window.__tpStok;
BroStat:这就是 TP 的状态机 先探测能力 再锁定最省请求的路径;
JavaScript:还有 tpDsPost 用 fetch 发 POST 请求;
JavaScript:混合包探测失败时会 console.warn 退回双包;
jumpLabel:converge;
label:branch_mi;
say:进入小米脚本的微分引擎;
JavaScript:小米的累计字节是一等事实源;
JavaScript:先拿 upload 和 download 拿不到累计量才允许瞬时速率接管;
BroStat:然后呢;
JavaScript:求导 用累计差除以时间差 得到真实带宽;
BroStat:计数器清零怎么办;
JavaScript:当前累计小于上次 就判定路由器重启 基线重锚 微分归零;
BroStat:还有十六分之一心跳;
JavaScript:对 F_ARR_16 把低于 8KB 的微弱流量映射成十六分之一的脉搏;
JavaScript:小米还有 Promise.all 并发拉取 devicelist 和 status;
BroStat:并发的结果统一以整批数据就绪时间为本轮时间;
jumpLabel:converge;
label:converge;
;[repo2gal performance] cue000004 b000070
changeBg:WebGAL_New_Enter_Image.webp -enter=shockwaveIn -enterDuration=1200;
say:两条路最终汇入同一套持久化;
BroStat:ha_snapshot 是共同的名字;
JavaScript:GM_setValue 写入 GM_getValue 读取;
JavaScript:快照里有全局 wan_up wan_down 还有每台设备的 integral_up;
BroStat:CSV 导出在周期边界触发;
JavaScript:doSettle 构造带 BOM 的 CSV 生成 Blob 点击下载;
JavaScript:还打开一个 about blank 窗口作为补下备份;
BroStat:周期类型支持每月 每周 固定天数;
JavaScript:calcTime 用 Date.UTC 算边界 Warn_MS 是预警 Force_MS 是强制结算;
JavaScript:时区补偿 28800000 是 UTC 加 8;
BroStat:还有 Home Assistant 联动;
JavaScript:Webhook 推送 HACS 集成 不再只靠一个浏览器观测;
JavaScript:每个脚本还有保活 iframe;
BroStat:tKA 让隐藏 iframe 定期访问系统路由;
JavaScript:对 后台会话不容易掉线;
BroStat:接下来看什么;
choose:CSV 结算细节:csv_branch|Home Assistant 联动:ha_branch;
label:csv_branch;
say:进入 doSettle 和 buildCSV;
JavaScript:buildCSV 生成带 BOM 的文本 首行是标题;
JavaScript:全局统计 设备明细 设备名称 MAC IP 接口 高精积分;
BroStat:每台设备的 integral_up 和 raw_up 都写进去;
JavaScript:doSettle 有防重入锁 S._RST;
JavaScript:写完 CSV 后把 gege_reset_ms 设成当前时间;
JavaScript:ha_snapshot 也清成空快照;
BroStat:内存原地清零;
JavaScript:对 WAN 总计清零 设备积分清零 环形缓冲也清零;
JavaScript:最后重算下一周期边界;
jumpLabel:scene_archive;
label:ha_branch;
say:进入 Home Assistant 联动;
JavaScript:ha_snapshot 存在 userscript 的 GM 存储里;
JavaScript:伴随 HACS 集成 可以通过 Webhook 推送;
BroStat:ZTE Stat HA 是兄弟项目;
JavaScript:对 它让状态可以被多端并发观测;
BroStat:快照里有 timestamp 和 devices;
JavaScript:每次 rUI 更新 cSnap 再交给 GM_setValue;
BroStat:这样即使网页关了 数据也有接力;
JavaScript:对 这是本地快照持久化;
jumpLabel:scene_archive;
label:scene_archive;
;[repo2gal performance] cue000005 b000105
changeBg:background-archive.png -enter=shockwaveIn -enterDuration=1200;
bgm:bgm-archive.ogg;
say:最后回到历史目录;
BroStat:history 目录里有华为后台频率测控探针 还有小米 5.9.3;
JavaScript:探针版本把 LAN 和 WAN 刷新间隔都设成 0.5 秒;
JavaScript:它还有一个企业级心跳雷达 十秒预热 统计间隔抖动;
BroStat:当前版把这个探针摘掉了;
JavaScript:对 当前华为脚本回到 2 秒间隔;
JavaScript:保留了 RSSI 漏桶防抖和物理网口 SVG;
BroStat:小米 5.9.3 呢;
JavaScript:fBy 还没有十六分之一心跳;
JavaScript:后来的版本才把微弱流量映射成十六分之一的脉搏;
BroStat:历史不是一蹴而就 是一轮一轮采样 一轮一轮结算;
say:脚本要一直读下去 这是对极客精神最好的尊重;
end;
